#!/usr/bin/env python
# This serf query starts the factomd docker container
import subprocess
import os

# Custom utils
from util import serfutil

if serfutil.isFactomd():
	subprocess.call(["docker", "start", "factomd"])
else:
	print("No factomd")